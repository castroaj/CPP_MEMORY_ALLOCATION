#include "../hdr/MemoryOperation.h"

MemoryOperation::MemoryOperation(int referenceNumber, int operation, int arguement)
    : m_referenceNumber(referenceNumber), m_operation(operation), m_arguement(arguement)
{}